# Скопіюй цей файл у ".env" і встав свій токен - .env у .gitignore і в git не потрапляє.
# Токен резервного джерела - офіційний API alerts.in.ua (https://api.alerts.in.ua/).
# Опційно: без нього скрипт просто працює лише через основне джерело (ubilling).
ALERTS_IN_UA_TOKEN=your_token_here
